// Движение блока
// const block = document.querySelector('#block');
// document.addEventListener('keydown', function(e) {
//   if (e.code === 'ArrowDown') {
//     block.style.top = block.offsetTop + 10 + 'px';
//   } else if (e.code === 'ArrowUp') {
//     block.style.top = block.offsetTop - 10 + 'px';
//   } else if (e.code === 'ArrowLeft') {
//     block.style.left = block.offsetLeft - 10 + 'px';
//   } else if (e.code === 'ArrowRight') {
//     block.style.left = block.offsetLeft + 10 + 'px';
//   }
// });
//
// function getRandom(max) {
//   return Math.floor(Math.random() * max);
// }

// setInterval(() => {
//   const y = window.innerHeight - block.offsetHeight;
//   const x = window.innerWidth - block.offsetWidth;
//
//   block.style.top = getRandom(y) + 'px';
//   block.style.left = getRandom(x) + 'px';
// }, 400);


// Поворот блока при помощи слайдера
// const slider = document.querySelector('#slider');
// const block = document.querySelector('#block');
// slider.addEventListener('input', function(e) {
//   block.style.transform = 'rotate(' + e.target.value + 'deg)';
// })


// const form = document.querySelector('#form');
// form.addEventListener('input', function(e) {
//   console.log(e.target.value);
// });






// Галерея
// Смена размера блока при вводе значений
// Появление блока при доскроле к нему
// Кнопка "вверх"
// [~~~] Перетягивание элементов (потянуть блок, ресайз за уголки)
